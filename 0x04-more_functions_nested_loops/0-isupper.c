#include "main.h"

/**
 * _isupper - function name
 * @c: checks if a character is upper
 * Return: 1
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
